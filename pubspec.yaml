name: flutter_app
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  carousel_pro: ^1.0.0
  webview_flutter: ^0.3.14+1
  font_awesome_flutter: 8.4.0

  firebase_core: ^0.3.4
  firebase_analytics: ^2.1.1+3

  firebase_auth: ^0.8.4+5 #firebase authentication
  google_sign_in: ^4.0.1+1

  cloud_firestore: ^0.10.1 #firebase database Store
  firebase_storage: ^2.0.0 #firebase storage

  rxdart: ^0.21.0
  provider: 3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/covers/
    - assets/logo.png
    - assets/congrats.gif

